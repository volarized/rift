rules:
  cache-poisoning:
    ignore:
      # Release jobs restore caches written only by protected branches and tags.
      - rift-release.yml
  unpinned-uses:
    config:
      policies:
        "*": ref-pin
