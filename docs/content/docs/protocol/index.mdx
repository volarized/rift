---
title: Protocol
description: The protocol behind Rift, and how the processes that implement it run.
---

import { Callout } from "fumadocs-ui/components/callout";
import { IsoDiagram } from "@/components/iso-diagram";

At the core of Rift there is a protocol. It describes how the components of the system behave, and how
they operate.

Rift reaches agents through MCP. Each agent talks to its own `rift mcp` process, over stdio.

That process is launched in a workspace — a distinct path on the filesystem of your machine:

```
~/projects/charming-aurora  ->  running rift mcp here
```

<Callout>
  An MCP server on the stdio transport is a subprocess of the client that launched it. Two agents means
  two `rift mcp` processes, and neither can see what the other is doing.
</Callout>

That is the constraint the rest of the design follows from. Several agents can be working in the same
workspace at once, and they need a central point behind them. That central point is the rift server.

<IsoDiagram
  alt="Two agents. Each talks to its own rift mcp process over stdio. Both rift mcp processes talk to a single rift server over Streamable HTTP on a Unix domain socket. The server owns the semantic DB, the git worktrees and the language adapters."
  chart={`
    flowchart LR
      agent1["agent 1"]:::agent -- "stdio" --> mcp1["rift mcp"]:::rift
      agent2["agent 2"]:::agent -- "stdio" --> mcp2["rift mcp"]:::rift
      mcp1 -- "Streamable HTTP<br/>over Unix socket" --> server["rift server"]:::rift
      mcp2 -- "Streamable HTTP<br/>over Unix socket" --> server
      server --> semantic["semantic DB"]:::store
      server --> worktrees["git worktrees"]:::git
      server --> adapters["adapters"]:::adapter
  `}
/>

So `rift mcp` is a proxy. It holds no state of its own — it forwards what the agent asks, and returns
what the server answers.

The hop behind it is Streamable HTTP over a Unix domain socket, and that is where the session lives. The
server assigns an
[`Mcp-Session-Id`](https://modelcontextprotocol.io/specification/2025-06-18/basic/transports#session-management)
at initialisation and keeps a table from that ID to the workspace the connection belongs to. The agent's
own stdio connection has no session ID, because MCP does not define one for stdio.

When the rift server starts, it scans the repository and builds the semantic level using the language
adapters.

## Where the protocol is written down

Three JSON Schema documents. Each page here renders one of them directly.

- [Core model](/docs/protocol/core) — what Rift reads, and what it can say about it. Read this one
  first; the other two are defined in terms of it.
- [MCP surface](/docs/protocol/mcp) — the tools and resources an agent calls.
- [Adapter wire](/docs/protocol/adapter) — how the server talks to one language's compiler.
