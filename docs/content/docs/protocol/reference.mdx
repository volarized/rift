---
title: Reference
---

import { Reference } from "@/components/protocol/reference";

Every type in the protocol, grouped by the axis it belongs to.

A type is listed once, under the first thing that refers to it. The sidebar carries that nesting, so
reading down a branch there follows the model.

<Reference />
