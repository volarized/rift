---
title: Reference
---

import { Reference } from "@/components/protocol/reference";

The reference follows the axis groups declared in `core.json`. Within each group, a type appears under
the first definition that refers to it, which keeps related shapes on the same branch.

<Reference />
