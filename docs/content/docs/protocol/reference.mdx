---
title: Reference
---

import { Reference } from "@/components/protocol/reference";

Grouped by the axis each type is filed on in `core.json`. A type is listed once, under the first thing
that refers to it, so reading down a branch follows the model rather than the alphabet.

<Reference />
