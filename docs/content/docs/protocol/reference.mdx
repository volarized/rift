---
title: Reference
---

import { Reference } from "@/components/protocol/reference";

Every type in the protocol, grouped by the axis it belongs to and nested under the type that reaches it.

A type is listed once, under the first thing that refers to it, so reading down a branch follows the
model. Types defined outside `core.json` carry a badge naming the document they come from.

<Reference />
