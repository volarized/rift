---
title: Protocol
description: The Rift contract — MCP tools and resources, the semantic model, the persistence models, and the language-adapter IPC.
---

import { ProtocolReference } from "@/components/protocol";

{/*
  The body is a component, not prose: it reads protocol/{core,mcp,adapter}.json
  and renders them. Nothing on this page is written by hand, so nothing on it
  can drift from the schemas. The table of contents and the search index are
  built from the same load — see `protocolToc` and `protocolStructuredData` in
  src/lib/protocol.ts.
*/}

<ProtocolReference />
