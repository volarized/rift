---
title: Rift
---

import { VersionRedirect } from "@/components/version-redirect";

<VersionRedirect />

The documentation is versioned. The current tree is the [draft](/docs/draft), which tracks the
protocol as it is written; a released version keeps its own dated snapshot alongside it.
