//!
//! AUTO-GENERATED CODE - DO NOT EDIT!
//!
//! Rift Global Package API
//! Source: docs/public/global-api.openapi.json
//! Version: 0.0.43
//! Generated by `oas3-gen v0.28.0`
//!
//! REST contract for package capabilities, resolution, declaration search, and symbol reads.

use anyhow::Context;
use reqwest::{Client, Url};
use validator::Validate;
use super::types::*;
pub const BASE_URL: &str = "https://example.com/";
#[derive(Debug, Clone)]
pub struct RiftGlobalPackageApiClient {
    pub client: Client,
    pub base_url: Url,
}
impl Default for RiftGlobalPackageApiClient {
    fn default() -> Self {
        Self::new()
    }
}
impl RiftGlobalPackageApiClient {
    /// Create a client using the OpenAPI `servers[0]` URL.
    #[must_use]
    #[track_caller]
    pub fn new() -> Self {
        Self {
            client: Client::builder().build().expect("client"),
            base_url: Url::parse(BASE_URL).expect("valid base url"),
        }
    }
    /// Create a client with a custom base URL.
    pub fn with_base_url(base_url: impl AsRef<str>) -> anyhow::Result<Self> {
        Ok(Self {
            client: Client::builder().build().context("building reqwest client")?,
            base_url: Url::parse(base_url.as_ref()).context("parsing base url")?,
        })
    }
    /// Create a client from an existing `reqwest::Client`.
    pub fn with_client(
        base_url: impl AsRef<str>,
        client: Client,
    ) -> anyhow::Result<Self> {
        let url = Url::parse(base_url.as_ref()).context("parsing base url")?;
        Ok(Self { client, base_url: url })
    }
    /// Read package API capabilities.
    ///
    /// Returns supported package managers, features, publication revisions, and active bounds.
    ///
    /// * Path: `GET /v1/capabilities`
    pub async fn get_capabilities(
        &self,
        request: GetCapabilitiesRequest,
    ) -> anyhow::Result<GetCapabilitiesResponse> {
        request.validate().context("parameter validation")?;
        let mut url = self.base_url.clone();
        url.path_segments_mut()
            .map_err(|()| anyhow::anyhow!("URL cannot be a base"))?
            .push("v1")
            .push("capabilities");
        let response = self.client.get(url).send().await?;
        Ok(GetCapabilitiesRequest::parse_response(response).await?)
    }
    /// Resolve package context.
    ///
    /// Classifies canonical exact versions and declared requirements against indexed package versions.
    ///
    /// * Path: `POST /v1/resolutions`
    pub async fn resolve_package_context(
        &self,
        request: ResolvePackageContextRequest,
    ) -> anyhow::Result<ResolvePackageContextResponse> {
        request.validate().context("parameter validation")?;
        let mut url = self.base_url.clone();
        url.path_segments_mut()
            .map_err(|()| anyhow::anyhow!("URL cannot be a base"))?
            .push("v1")
            .push("resolutions");
        let response = self.client.post(url).json(&request.body).send().await?;
        Ok(ResolvePackageContextRequest::parse_response(response).await?)
    }
    /// Search package declarations.
    ///
    /// Returns one ranked declaration page from selected exact package versions.
    ///
    /// * Path: `POST /v1/search`
    pub async fn search_packages(
        &self,
        request: SearchPackagesRequest,
    ) -> anyhow::Result<SearchPackagesResponse> {
        request.validate().context("parameter validation")?;
        let mut url = self.base_url.clone();
        url.path_segments_mut()
            .map_err(|()| anyhow::anyhow!("URL cannot be a base"))?
            .push("v1")
            .push("search");
        let response = self
            .client
            .post(url)
            .query(&request.query)
            .json(&request.body)
            .send()
            .await?;
        Ok(SearchPackagesRequest::parse_response(response).await?)
    }
    /// List package symbols.
    ///
    /// Returns one declaration page from selected exact package versions.
    ///
    /// * Path: `POST /v1/symbols`
    pub async fn list_package_symbols(
        &self,
        request: ListPackageSymbolsRequest,
    ) -> anyhow::Result<ListPackageSymbolsResponse> {
        request.validate().context("parameter validation")?;
        let mut url = self.base_url.clone();
        url.path_segments_mut()
            .map_err(|()| anyhow::anyhow!("URL cannot be a base"))?
            .push("v1")
            .push("symbols");
        let response = self
            .client
            .post(url)
            .query(&request.query)
            .json(&request.body)
            .send()
            .await?;
        Ok(ListPackageSymbolsRequest::parse_response(response).await?)
    }
}

