name: integration

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: integration-${{ github.ref }}
  cancel-in-progress: false

jobs:
  integration-build:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - uses: Swatinem/rust-cache@v2.9.2
        with:
          shared-key: integration
      - uses: taiki-e/install-action@v2.86.3
        with:
          tool: just,cargo-llvm-cov@0.8.7,cargo-nextest@0.9.140
      - name: Build and archive integrations
        run: just integration-archive
      - uses: actions/upload-artifact@v6.0.0
        with:
          name: integration-build
          path: |
            target/integration.tar.zst
            target/integration-cli.tar.zst
          compression-level: 0
          if-no-files-found: error
          retention-days: 7

  corpus:
    needs: integration-build
    strategy:
      fail-fast: false
      matrix:
        include:
          - repository: fastapi
            case: workspace
            test: test_fastapi_corpus
            minutes: 5
          - repository: bun
            case: workspace
            test: test_bun_workspace
            minutes: 10
          - repository: bun
            case: stop
            test: test_bun_stop
            minutes: 10
          - repository: nextjs
            case: workspace
            test: test_nextjs_corpus
            minutes: 13
          - repository: nextjs
            case: churn
            test: test_nextjs_churn
            minutes: 13
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@v7.6.0
        with:
          enable-cache: true
          cache-dependency-glob: dev/uv.lock
      - uses: actions/download-artifact@v7.0.0
        with:
          name: integration-build
          path: target
      - uses: taiki-e/install-action@v2.86.3
        with:
          tool: just,cargo-llvm-cov@0.8.7,cargo-nextest@0.9.140
      - run: rustup component add llvm-tools-preview
      - name: Install validating client
        run: uv sync --locked --python 3.12 --project dev
      - uses: actions/cache@v6.1.0
        with:
          path: ~/.cache/huggingface/hub-corpus
          key: corpus-${{ matrix.repository }}-${{ hashFiles('crates/rift/tests/corpus/pins.toml') }}
      - name: Fetch pinned repository
        run: just corpus-sync "${{ matrix.repository }}"
      - name: Run corpus suite
        id: corpus
        timeout-minutes: ${{ matrix.minutes }}
        env:
          RIFT_CORPUS_REPORT: ${{ github.workspace }}/target/corpus/${{ matrix.repository }}.json
        run: just corpus-test "${{ matrix.repository }}" "${{ matrix.test }}" target/integration.tar.zst
      - name: Report corpus coverage
        if: ${{ !cancelled() && steps.corpus.outcome != 'skipped' }}
        run: cargo llvm-cov report --nextest-archive-file target/integration.tar.zst --lcov --output-path target/corpus.lcov
      - uses: codecov/codecov-action@v5.5.5
        if: ${{ !cancelled() && steps.corpus.outcome != 'skipped' }}
        with:
          disable_search: true
          fail_ci_if_error: true
          files: target/corpus.lcov
          flags: corpus
          name: corpus-${{ matrix.repository }}-${{ matrix.case }}
          token: ${{ secrets.CODECOV_TOKEN }}
      - uses: codecov/codecov-action@v5.5.5
        if: ${{ !cancelled() && steps.corpus.outcome != 'skipped' }}
        with:
          disable_search: true
          fail_ci_if_error: true
          files: target/nextest/corpus/junit.xml
          flags: gate
          name: corpus-${{ matrix.repository }}-${{ matrix.case }}
          report_type: test_results
          token: ${{ secrets.CODECOV_TOKEN }}
      - uses: actions/upload-artifact@v6.0.0
        if: always()
        with:
          name: corpus-${{ matrix.repository }}-${{ matrix.case }}
          path: |
            target/corpus/
            target/corpus.lcov
            target/nextest/corpus/junit.xml
          if-no-files-found: warn
          retention-days: 7

  live:
    needs: integration-build
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - uses: actions/download-artifact@v7.0.0
        with:
          name: integration-build
          path: target
      - uses: astral-sh/setup-uv@v7.6.0
        with:
          enable-cache: true
          cache-dependency-glob: dev/uv.lock
      - uses: taiki-e/install-action@v2.86.3
        with:
          tool: just,cargo-llvm-cov@0.8.7,cargo-nextest@0.9.140
      - run: rustup component add llvm-tools-preview rust-analyzer
      - uses: oven-sh/setup-bun@v2.2.0
        with:
          bun-version: latest
      - uses: actions/cache@v6.1.0
        with:
          path: |
            ~/.bun/install/cache
            ~/.cache/huggingface/hub
          key: integration-dependencies-${{ runner.os }}-${{ hashFiles('dev/uv.lock', 'Cargo.lock', 'tools/mcp-conformance/bun.lock') }}
          restore-keys: integration-dependencies-${{ runner.os }}-
      - run: uv sync --locked --python 3.12 --project dev
      - run: uv tool install tombi
      - name: Run live integrations
        id: live
        run: just live-test target/integration.tar.zst
      - name: Report integration coverage
        if: ${{ !cancelled() && steps.live.outcome != 'skipped' }}
        run: cargo llvm-cov report --nextest-archive-file target/integration.tar.zst --lcov --output-path target/integration.lcov
      - name: Extract compiled CLI
        run: |
          mkdir -p target/integration-cli
          tar --zstd -xf target/integration-cli.tar.zst -C target/integration-cli
      - name: Validate artifact and MCP client
        run: |
          just artifact-test --binary target/integration-cli/rift --junit target/integration/artifact.xml
          just agent-test --binary target/integration-cli/rift --junit target/integration/agent.xml
          just conformance target/integration-cli/rift
      - name: Validate cold first use
        run: just coldstart-test --binary target/integration-cli/rift --junit target/integration/coldstart.xml
      - uses: codecov/codecov-action@v5.5.5
        if: ${{ !cancelled() && steps.live.outcome != 'skipped' }}
        with:
          disable_search: true
          fail_ci_if_error: true
          files: target/integration.lcov
          flags: integration
          token: ${{ secrets.CODECOV_TOKEN }}
      - uses: actions/upload-artifact@v6.0.0
        if: always()
        with:
          name: integration-results
          path: |
            target/integration/
            target/integration.lcov
            target/nextest/integration/junit.xml
          if-no-files-found: error
          retention-days: 7
